// Nextflow configuration

profiles {
    standard {
        process.executor = 'local'
    }
    docker {
        docker.enabled = true
    }
}

// Resource limits for Macbook
process {
    cpus = 2
    memory = '8 GB'
}